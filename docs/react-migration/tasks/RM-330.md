# RM-330 — Port charts and custom icon override support

| Field           | Value           |
| --------------- | --------------- |
| Owner           | Claude          |
| Status          | `DONE`          |
| Dependency      | RM-210 (`DONE`) |
| Baseline commit | `4cae693`       |
| Final commit    | `382ff6c`       |
| Date            | 2026-07-14      |

## Outcome

- Added `src/components/charts/DonutChart.tsx` with the legacy 96-viewBox ring,
  Arabic-Indic percentage, 0–100 clamping, track-only zero state, and 112/88 px
  sizes.
- Added `src/components/charts/SegmentBar.tsx` with proportional fills, 2 px
  gaps, zero-fill suppression, and a complete text legend.
- Chart colors resolve through the MUI palette using a closed
  `primary | accent | neutral` semantic union.
- Added the `src/components/icons/` source/store and `useIconUrl`, preserving
  `BASE_URL` paths and PNG-over-SVG overrides.
- Updated `NavIcon` and both React entries to consume/start the override system.
- Kept legacy modules, production entries, data/domain code, dependencies, and
  the lockfile unchanged.

## Intentional behavior decisions

- The React icon probe requires an `image/*` content type. This avoids Vite's
  missing-PNG `200 text/html` SPA fallback; production behavior is unchanged.
- Accessible donut percentages use Arabic-Indic digits and are announced once.
- Charts remain presentational. RM-500 and RM-530 own real admin data wiring.

## Verification

- `npm run typecheck` — passed.
- `npm run lint` — passed.
- `npm test` — 27 files / 149 tests passed; gated emulator test skipped.
- `npm run build` — passed; production inputs remain legacy-only.
- `npm run check:bundle-budgets` — passed:
  - member 322.52/350 kB JS, 368.63/400 kB transfer;
  - admin 322.50/375 kB JS, 368.61/425 kB transfer.
- Changed files were Prettier-clean and `git diff --check` passed.
- Live mobile and desktop RTL checks passed in both React previews.
- A temporary `public/icons/personal.png` overrode both navs live and removal
  reverted them to SVG.

## Handoff

RM-340 and RM-350 are both unblocked and complete Phase 3. RM-340 is recommended
first because Phase 4 identity, settings, completion, and reader work depend on
its persistence hooks.
