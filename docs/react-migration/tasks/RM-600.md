# RM-600 — Cut over member entry to React

- Status: DONE
- Owner: Joint (this pass driven by Codex)
- Dependencies: RM-460 (complete)
- Baseline commit: `6c733e9` (RM-560 + RM-570 clean handoff)

## Scope

Replace the legacy member bootstrap used by the production `index.html` input
with `src/app/entries/member.tsx`. Keep the separate production member/admin
build inputs and the hidden admin entry unchanged. Verify that the production
member page mounts the React surface, the production build succeeds, and the
member bundle remains within the RM-040 budgets (350 kB initial JS gzip / 400 kB
initial transfer).

## Acceptance criteria

- `index.html` loads `src/app/entries/member.tsx`.
- The production member page mounts the React member experience.
- The hidden production admin entry and both Vite production inputs are unchanged.
- Typecheck, production build, member smoke, and bundle-budget checks pass.

## Verification

- `npm run typecheck` — passed as the pre-change baseline.
- `npx vitest run tests/tooling/production-entries.test.ts` — 1 file, 4 tests
  passed; at the RM-600 checkpoint the contract required production member →
  React while production admin remained legacy, and kept both preview files out
  of production inputs.
- `npm run build` — passed; `dist/index.html` emitted with the React member
  chunk (`member-2QdUJkKr.js`, 593.75 kB raw / 187.94 kB gzip) while the hidden
  admin page still emitted its legacy chunk.
- Built-page member smoke at `/` — React/MUI surface rendered (`main`, Emotion
  styles, heading `ختماتي`), `lang="ar"`, `dir="rtl"`, runtime title `خَتْمة`,
  emitted member module loaded, and no console warnings/errors.
- `npm run check:bundle-budgets` — passed: member initial JS **341.62 kB**
  (limit 350 kB), initial transfer **387.73 kB** (limit 400 kB). Admin also
  remained within its unchanged limits.

## Decisions and risks

- The React preview stays available as a development-only comparison surface
  until legacy cleanup (RM-620); RM-600 changes only the production member
  bootstrap.
- The admin production bootstrap remains legacy until RM-610.
- The React entry now sets `document.title` from the centralized Arabic strings,
  preserving the inventory §1.1 runtime-title contract previously owned by the
  legacy bootstrap.
