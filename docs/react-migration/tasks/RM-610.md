# RM-610 — Cut over admin entry to React

- Status: DONE
- Owner: Joint (this pass driven by Codex)
- Dependencies: RM-570, RM-600 (complete)
- Baseline: `6c733e9` + completed RM-600 working tree

## Scope

Replace the legacy admin bootstrap used by the hidden production
`admin-nano.html` input with `src/app/entries/admin.tsx`. Preserve the unguessable
filename, `noindex` metadata, absence of member-app links, and the two production
Vite inputs. Verify the built hidden route mounts the React admin surface without
entry or route regression and remains within the RM-040 admin budgets.

## Acceptance criteria

- `admin-nano.html` loads `src/app/entries/admin.tsx` and retains its security
  metadata and deployment warning.
- Both production HTML inputs mount React; preview HTML remains excluded from
  the production build inputs.
- The built hidden admin URL mounts the React admin experience and preserves a
  valid admin hash route.
- Typecheck, focused entry tests, production build, admin smoke, and bundle-budget
  checks pass.

## Verification

- `npx vitest run tests/tooling/production-entries.test.ts` — 1 file, 4 tests
  passed; the contract now requires both production pages → React while keeping
  both preview HTML files out of production inputs.
- `npm run build` — passed; both production HTML inputs emitted with React entry
  chunks. The hidden page emitted `admin-CH98pSlb.js` (34.53 kB raw / 9.41 kB
  gzip) plus the shared React bootstrap chunk.
- Built-page admin smoke at `/admin-nano.html#/home` — `#/home` resolved to the
  React admin surface/`home` route with heading `نظرة عامة`; `lang="ar"`,
  `dir="rtl"`, `noindex, nofollow`, runtime title `خَتْمة — لوحة التحكم`, emitted
  admin module loaded, and no console warnings/errors.
- `npm run check:bundle-budgets` — passed: admin initial JS **342.32 kB**
  (limit 375 kB), initial transfer **388.43 kB** (limit 425 kB). Member remained
  within its 350/400 kB limits.
- `npm run lint` — passed.
- `npm test` — 41 files passed, 1 skipped; 235 tests passed, 1 skipped.
- Prettier check and `git diff --check` — clean after formatting the two touched
  production HTML files and the Phase 6 tracker table.

## Decisions and risks

- The hidden filename remains an operator-configured obscurity gate; this task
  does not add authentication or expose a link from the member app.
- Legacy source and Tailwind remain present until RM-620; RM-610 changes only the
  production admin bootstrap and its cutover contracts.
- The React admin entry sets `document.title` from the centralized Arabic
  strings, preserving the inventory §1.1 runtime-title contract.
