# RM-670 — Update all project documentation

- Status: DONE
- Owner: Joint (this pass driven by Claude)
- Dependencies: RM-620, RM-630 (complete)
- Baseline commit: `5cf3d8c` (RM-640 + RM-650 handoff)
- Final commit: `16563f9`

## Outcome / scope

Reconcile the living user/developer documentation with the shipped React + MUI
implementation and the current local/emulator setup. This is a documentation-only
task: no source code changes. Fix statements and file/path references that no
longer match the final implementation after Phase 4–6 (React cutover, legacy/
Tailwind removal, bundle split, persistence hooks, validation/content modules).

Out of scope: rewriting the labeled point-in-time migration artifacts at the repo
root (`REACT_MIGRATION_UI_INVENTORY.md` RM-020, `REACT_MIGRATION_THEME_MAP.md`
RM-115, `REACT_MIGRATION_DEPENDENCY_AUDIT.md` RM-110). RM-620 explicitly permits
those historical parity oracles/audits to keep naming deleted legacy files; they
carry their own task id, baseline commit, and date, so they read as history, not
current guidance. `REACT_MIGRATION_PLAN.md` is a still-correct router into
`docs/react-migration/` and is left as-is.

## Acceptance criteria

- No living project doc claims the app is plain HTML + Tailwind or "no framework".
- README, ARCHITECTURE, PROGRESS, REQUIREMENTS, and the CLAUDE.md file map match
  the final `src/` layout, data model, and setup flow.
- No living doc links to a deleted source file.
- Documentation-only verification passes: link/path review, formatter, and
  `git diff --check`.

## Context to read

- `docs/react-migration/NOW.md`, TRACKER Phase 6, tasks RM-620/RM-630/RM-640/RM-650.
- `PLAN.md` → Verification matrix row "Documentation only".
- Source of truth checked against docs: `src/domain/types.ts`, `vite.config.ts`,
  `package.json`, and the real `src/` tree.

## Files or ownership boundaries

Living docs reconciled:

- `REQUIREMENTS.md` — §3 tech stack (UI/theming/architecture) migrated note.
- `ARCHITECTURE.md` — directory map + Firestore data model completeness.
- `PROGRESS.md` — deleted-file link + status/next-steps framing.
- `README.md` — spot check (already React-accurate).
- `CLAUDE.md` — file map completeness only.

## Decisions and risks

- REQUIREMENTS §3 is a requirements doc; rather than delete the original intent,
  it gets a short "React migration" note plus updated UI/theming/architecture
  bullets, preserving the unchanged static-site / no-server / Firestore / layered
  requirements. This keeps older migration docs that mention Tailwind coherent.
- Data-model additions (Khatma `capacities`, RoundChunk `loosePages`/
  `redistributedPages`, the `content/global` collection) mirror `types.ts`, which
  names ARCHITECTURE.md and CLAUDE.md as its schema reference.

## Verification

Documentation-only per the PLAN verification matrix (no source changed):

- **Reconciled** `REQUIREMENTS.md` §3 (React + MUI note and updated UI/theming/
  presentation bullets), `ARCHITECTURE.md` (directory map + `Khatma.capacities`,
  `RoundChunk.loosePages`/`redistributedPages`, the `content/global` collection,
  and a restored `## Security` section), `PROGRESS.md` (deleted `schedule.ts`
  link removed; Next Steps repointed at the tracker), and the `CLAUDE.md` file
  map (`persistence`, `content.ts`, `validation.ts`, `fonts/`).
- **Link/path review** — a script resolved every relative markdown link in the
  edited docs to a real file (0 broken). The `#security` anchor in README (used
  twice) resolves again now that the section is restored; `#admin-url--security`
  and `#enforced-boundaries-guardrails` map to real headings.
- **Formatter** — `npx prettier --check` passes for all edited docs.
- **Whitespace** — `git diff --check` clean (only informational LF→CRLF notices).

## Final handoff

- Living user/developer docs now describe the shipped React + MUI app, the final
  `src/` layout, the complete Firestore data model, and the current setup/next
  steps. No living doc claims plain-HTML/Tailwind or links to a deleted file.
- Root `REACT_MIGRATION_*.md` files are intentionally untouched: `*_PLAN.md` is a
  correct router into `docs/react-migration/`; the UI-inventory / theme-map /
  dependency-audit files are labeled RM-020/RM-115/RM-110 point-in-time oracles
  that RM-620 permits to name deleted legacy files.
- With RM-670 DONE, RM-700 (final clean quality suite) is unblocked; RM-660
  remains authorization-gated.
