# RM-700 — Final clean quality suite

- Status: DONE
- Owner: Joint (this pass driven by Claude)
- Dependencies: RM-630, RM-640, RM-650, RM-670 (all DONE)
- Baseline commit: `fda6c41` (RM-670 handoff hash)
- Final commit: _recorded in the follow-up handoff-hash commit_

## Scope

First task of Phase 7 (merge readiness). Run the complete quality suite from a
clean install to prove the migrated app is green end to end before the delta
review (RM-710) and joint review (RM-720). No source changes are expected; the
only edits are the tracker/task/NOW bookkeeping. If a real failure surfaces, stop
and record a separate defect task rather than patching source under this QA task.

## Acceptance criteria

- Clean install from the committed lockfile (`npm ci`) succeeds.
- `npm run typecheck`, `npm run lint`, `npm test` (all tests), and `npm run build`
  all pass, mirroring the CI job in `.github/workflows/ci.yml`.
- The CI-equivalent sequence passes locally on the pinned Node 24 LTS line.
- Final bundle budgets (`npm run check:bundle-budgets`) still pass (merge-readiness
  gate "Final bundle sizes are measured and accepted").
- Results are recorded here; no unrelated source change is introduced.

## Context to read

- `docs/react-migration/NOW.md`, TRACKER Phase 7, `PLAN.md` verification matrix
  (row "Cutover/removal") and merge-readiness gates.
- `.github/workflows/ci.yml` — the canonical CI sequence this task mirrors.
- `package.json` scripts + `engines`, `.nvmrc` — pinned Node 24 LTS line.

## Environment note

- `.nvmrc` and `package.json` `engines.node` pin `24.18.0`; the local runner is
  `v24.14.0` (same Node 24 LTS line, minor behind). Recorded for transparency;
  CI uses `node-version-file: .nvmrc`, so CI runs exactly `24.18.0`.

## Verification

Full CI-equivalent suite run from a clean install on the local Node 24 LTS runner
(`v24.14.0`); mirrors `.github/workflows/ci.yml` step for step, plus the bundle
gate. Every step exited 0.

| Step           | Command                                                  | Result                                                   |
| -------------- | -------------------------------------------------------- | -------------------------------------------------------- |
| Clean install  | `npm ci`                                                 | Added 1032 packages / audited 1033 in ~55 s; exit 0.     |
| Typecheck      | `npm run typecheck` (`tsc --noEmit`)                     | Clean; exit 0.                                           |
| Lint           | `npm run lint` (`eslint .`)                              | Clean; exit 0.                                           |
| Tests          | `npm test` (`vitest run`)                                | 39 files passed, 1 skipped; 225 tests passed, 1 skipped. |
| Build          | `VITE_FIREBASE_PROJECT_ID=demo-khatma npm run build`     | Built in 412 ms; no oversized-chunk warning; exit 0.     |
| Bundle budgets | `node scripts/check-bundle-budgets.mjs` (vs. that build) | Both surfaces within budget; exit 0.                     |
| Whitespace     | `git diff --check`                                       | Clean (informational LF→CRLF notices only).              |

Final initial-load totals from the production manifest:

| Surface | Initial JS gzip | Initial transfer | JS budget | Transfer budget |
| ------- | --------------: | ---------------: | --------: | --------------: |
| Member  |       341.59 kB |        387.75 kB | 350.00 kB |       400.00 kB |
| Admin   |       344.72 kB |        390.98 kB | 375.00 kB |       425.00 kB |

Member headroom is 8.41 kB JS gzip / 12.25 kB transfer; admin headroom is
30.28 kB JS gzip / 34.02 kB transfer. Sizes match RM-630's recorded graph within
content-hash drift (member +0.15 kB, admin +0.15 kB vs. RM-630); no chunk crosses
Vite's 500 kB raw warning (largest is `vendor-firebase` at 483.78 kB raw).

## Notes / non-blocking observations

- `npm ci` prints `EBADENGINE`: the lockfile/`engines` pin `24.18.0` while the
  local runner is `v24.14.0` (same Node 24 LTS line). CI uses
  `node-version-file: .nvmrc`, so CI itself runs exactly `24.18.0`. Not a defect;
  no source change made for it.
- `npm audit` reports 9 moderate advisories in the dev/tooling tree (Firebase
  tooling chain). This is the accepted "Firebase tooling dev advisories" risk in
  `PLAN.md` (production audit clean; monitor rather than force breaking fixes).
  No dependency change made under this QA task.
- The `Not implemented: Window's scrollTo()` lines during tests are benign jsdom
  environment notices, not failures.

## Outcome

The migrated app is green end to end from a clean install: install, typecheck,
lint, all tests, build, and bundle budgets pass, mirroring CI on the Node 24 LTS
line. No source changes were required. RM-700 is DONE; RM-710 (review delta
against current `main`) is unblocked.
