# RM-710 — Review delta against current `main`

- Status: DONE
- Owner: Joint (this pass driven by Claude)
- Dependencies: RM-700 (DONE)
- Baseline commit: `8026f8c` (RM-700 work) / branch head `f32c847`
- Final commit: `d1c47e8` (handoff hash recorded in the follow-up commit)

## Scope

Reconcile every change on `reactmigration` since the migration base `6992007`
against the current `main`, and confirm the delta introduces no unrelated
overwrite before the joint code/behavior review (RM-720) and merge-summary
(RM-730). Review-only: no source changes.

## Acceptance criteria

- All changes since base `6992007` are accounted for and are intentional
  migration work.
- No change overwrites unrelated work that landed on `main` after the base.
- Preserved layers (domain, data, Quran content, Firestore rules/indexes) are
  confirmed unchanged, per PLAN scope.
- Findings recorded; nothing merged and no source modified.

## Git topology (read-only)

| Fact                                      | Value                                      |
| ----------------------------------------- | ------------------------------------------ |
| Migration base                            | `6992007` "automate firebase rules deploy" |
| `main` HEAD (local and `origin/main`)     | `6992007` — **unchanged since base**       |
| `reactmigration` HEAD                     | `f32c847`                                  |
| `merge-base(main, reactmigration)`        | `6992007` (= base = current `main`)        |
| Base is ancestor of `reactmigration`      | Yes — linear descent                       |
| Commits on `reactmigration` since base    | 56                                         |
| Commits on `main` not in `reactmigration` | **0** — nothing to reconcile               |
| Merge shape into `main`                   | Guaranteed conflict-free **fast-forward**  |

Because `main` never advanced past the base, there is no divergent `main` work
that this branch could overwrite. The PLAN risk "`main` advances during migration"
did not materialize; RM-710 confirms rather than repairs.

## Delta summary — 226 files (+20,847 / −4,352): 179 A, 28 D, 19 M

- **Added (179):** React application (`src/app` 70, `src/components` 28,
  `src/theme` 5), React tests (`tests/app` 26 + component/theme/tooling suites),
  `docs/react-migration/` (32), toolchain (`.nvmrc`,
  `scripts/check-bundle-budgets.mjs`), dev-only preview entries
  (`react-preview.html`, `admin-react-preview.html`), and the labeled root
  `REACT_MIGRATION_*.md` historical artifacts.
- **Deleted (28):** legacy only — the whole `src/ui/` DOM tree (admin/member/
  shared), legacy entry helpers `src/admin.ts`/`src/member.ts`,
  `src/theme/theme.css`, and two legacy UI tests (`tests/ui/admin-routes.test.ts`,
  `tests/ui/router.test.ts`). This is the RM-620 legacy/Tailwind removal.
- **Modified (19):** toolchain/config (`ci.yml`, `deploy.yml`, `.gitignore`,
  `eslint.config.js`, `tsconfig.json`, `vite.config.ts`, `vitest.config.ts`,
  `package.json`, `package-lock.json`), living docs (`ARCHITECTURE`, `CLAUDE`,
  `PROGRESS`, `README`, `REQUIREMENTS`), the two production entry HTMLs
  (`index.html`, `admin-nano.html`), `src/theme/reading.ts`, and two
  content-adjacent files reviewed below.

## Preserved-boundary verification

- `src/domain/` — **untouched** (absent from the diff). Pure logic preserved.
- `src/data/` — **untouched**. Firebase boundary preserved.
- `src/content/quran/` — **unchanged**. No Quran data/semantic change.
- `firestore.rules`, `firestore.indexes.json` — **unchanged**. No schema/rule
  change, matching PLAN "not in scope".
- `src/content/strings.ar.ts` (M) — **purely additive** UI strings: `confirm*`/
  `cancel`/`increase`/`decrease`, a `feedback` block (RM-300 primitives), and a
  `preview` block (dev previews). No existing string altered; no Quran text.
- `scripts/seed-emulator.ts` (M) — **comment-only** correction so the Round 2
  narrative matches invariant 2 (engine never auto-releases a held chunk). No
  seeded-data or behavior change.
- `.gitignore` (M) — adds `.claude/settings.local.json`. Benign.

## Hygiene checks

- No secret/credential/`.env`/key/binary/source-map files added.
- `dist/` is gitignored and not committed; no `node_modules` committed.
- OD-01 honored: `vite.config.ts` builds only `entryFiles.production`
  (`index.html`, `admin-nano.html`); the preview HTMLs are dev-only and cannot be
  published by `npm run build` (config comment + RM-700 build output confirm only
  the two production entries reach `dist/`). They are intentional, not dead code.

## Carry-forward to RM-720 (joint review), not blockers

- Confirm the two dev preview entries and the root `REACT_MIGRATION_*.md`
  artifacts are the intended final retained set (they are dev/historical by
  design, not orphaned legacy).
- OD-04 (merge method) remains OPEN for RM-740; the branch is fast-forwardable, so
  any chosen method (FF, merge commit, squash, or reviewed PR) applies without
  content conflicts.

## Outcome

The full delta since `6992007` is intentional migration work. `main` has not
advanced, so there is no unrelated overwrite; preserved layers are confirmed
untouched, and the branch fast-forwards into `main` conflict-free. RM-710 is DONE;
RM-720 (joint code/behavior review) is unblocked.
