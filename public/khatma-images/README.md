# Khatma series images

Add `.avif`, `.gif`, `.jpg`, `.jpeg`, `.png`, `.svg`, or `.webp` files to this
folder. Restart the dev server (or rebuild the app), and the images will appear
automatically in the admin series-image picker. Keep `placeholder.svg`; it is the
fallback shown when a series has no selected image.
